# vulnscan
VulnScan is a single-file, fully self-contained Linux vulnerability scanner designed for bug bounty hunters and penetration testers. It embeds LinPEAS directly inside the script, runs a full system enumeration, and then cross-references every finding against live CVE databases — all without needing root privileges.
